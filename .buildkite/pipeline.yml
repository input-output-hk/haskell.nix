steps:
  - label: 'Run tests'
    command: "./test/tests.sh"
    agents:
      system: x86_64-linux

  - label: 'Update docs'
    command:
      - nix-build -A maintainer-scripts.update-docs -o update-docs.sh
      - ./update-docs.sh
    branches: master
    agents:
      system: x86_64-linux
